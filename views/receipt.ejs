<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8">
  <title>
    <%= title %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Courier New', Courier, monospace;
      background-color: #f8f9fa;
    }

    .receipt {
      max-width: 400px;
      margin: 40px auto;
      padding: 20px 25px;
      background: white;
      border: 1px dashed #888;
      border-radius: 12px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.06);
    }

    .receipt h2 {
      font-size: 1.5rem;
      margin-bottom: 0.2rem;
      font-weight: bold;
    }

    .receipt small {
      font-size: 0.85rem;
      color: #666;
    }

    .receipt hr {
      border-top: 1px dashed #bbb;
      margin: 0.75rem 0;
    }

    .item-line {
      font-size: 0.95rem;
      line-height: 1.4;
    }

    .total-line {
      font-size: 1.1rem;
      font-weight: bold;
      text-align: right;
      margin-top: 1rem;
    }

    .receipt-footer {
      text-align: center;
      font-size: 0.9rem;
      margin-top: 1.5rem;
      color: #888;
    }

    .btn-print {
      display: block;
      margin: 25px auto 10px;
    }

    @media print {

      .btn-print,
      .btn-back {
        display: none;
      }

      body {
        background: white !important;
      }

      .receipt {
        border: none;
        box-shadow: none;
        margin: 0;
      }
    }
  </style>
</head>

<body>
  <!-- กล่องใบเสร็จ -->
  <div class="receipt text-center">
    <h2>🍽️ RESTAURANT</h2>
    <small>ใบเสร็จการสั่งซื้อ</small>
    <p class="mt-2">วันที่: <%= order.createdAt.toLocaleString() %>
    </p>
    <hr>

    <div class="text-start">
      <% let total=0; %>
        <% order.items.forEach(item=> { %>
          <% if (item.menu) { %>
            <% const sum=item.quantity * item.menu.price; total +=sum; %>
              <div class="item-line">
                <strong>
                  <%= item.menu.name %>
                </strong><br>
                <span>จำนวน: <%= item.quantity %> x <%= item.menu.price %> บาท</span><br>
                <span>รวม: <%= sum %> บาท</span>
              </div>
              <hr>
              <% } %>
                <% }) %>

                  <div class="total-line">
                    รวมทั้งหมด: <%= total %> บาท
                  </div>
    </div>

    <div class="receipt-footer">
      ขอบคุณที่อุดหนุน<br>
      — กรุณาเก็บใบเสร็จนี้ไว้ —
    </div>
  </div>

  <!-- ปุ่มพิมพ์ใบเสร็จ และปุ่มย้อนกลับ -->
  <div class="text-center">
    <button onclick="window.print()" class="btn btn-primary btn-print">
      🖨️ พิมพ์ใบเสร็จ
    </button>
    <a href="/history" class="btn btn-outline-secondary btn-back mt-2">ย้อนกลับ</a>
  </div>
</body>

</html>