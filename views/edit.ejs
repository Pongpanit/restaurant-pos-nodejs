<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    body {
      background-color: #f8f9fa;
      color: #212529;
      transition: background-color 0.3s, color 0.3s;
    }

    .form-container {
      background-color: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    }

    .form-group label {
      font-weight: 500;
    }

    .btn {
      padding: 10px 20px;
    }

    .img-preview {
      width: 200px;
      height: auto;
      margin-top: 10px;
      border-radius: 8px;
    }

    /* 🌙 Dark Mode */
    .dark-mode {
      background-color: #121212 !important;
      color: #f1f1f1 !important;
    }

    .dark-mode .form-container {
      background-color: #1e1e1e;
      color: white;
      border: 1px solid #333;
    }

    .dark-mode .form-control,
    .dark-mode select {
      background-color: #2a2a2a;
      color: white;
      border: 1px solid #444;
    }

    .dark-mode .form-control::placeholder {
      color: #bbb;
    }

    .dark-mode textarea.form-control {
      background-color: #000000;
      color: white;
    }

    .dark-mode .btn-primary {
      background-color: #0d6efd;
      border: none;
    }

    .dark-mode .btn-success {
      background-color: #28a745;
      border: none;
    }

    .dark-mode .btn:hover {
      filter: brightness(1.1);
    }
  </style>
</head>

<body>
  <%- include('navbar.ejs') %>

  <div class="container my-5">
    <div class="mx-auto form-container col-md-8">
      <h4 class="mb-4 fw-bold">แก้ไขเมนูอาหาร</h4>

      <form method="POST" action="/update" enctype="multipart/form-data">
        <input type="hidden" name="id" value="<%= menu._id %>">

        <div class="mb-3">
          <label>ชื่อเมนูอาหาร</label>
          <input type="text" class="form-control" name="name" value="<%= menu.name %>">
        </div>

        <div class="mb-3">
          <label>ราคา</label>
          <input type="text" class="form-control" name="price" value="<%= menu.price %>">
        </div>

        <div class="mb-3">
          <label>หมวดหมู่อาหาร</label>
          <select class="form-select" name="category">
            <option value="อาหารเรียกน้ำย่อย" <%= menu.category === 'อาหารเรียกน้ำย่อย' ? 'selected' : '' %>>อาหารเรียกน้ำย่อย</option>
            <option value="อาหารจานหลัก" <%= menu.category === 'อาหารจานหลัก' ? 'selected' : '' %>>อาหารจานหลัก</option>
            <option value="เครื่องเคียง" <%= menu.category === 'เครื่องเคียง' ? 'selected' : '' %>>เครื่องเคียง</option>
            <option value="อาหารหวาน" <%= menu.category === 'อาหารหวาน' ? 'selected' : '' %>>อาหารหวาน</option>
            <option value="เครื่องดื่ม" <%= menu.category === 'เครื่องดื่ม' ? 'selected' : '' %>>เครื่องดื่ม</option>
          </select>
        </div>

        <div class="mb-3">
          <label>ภาพเมนู</label>
          <input type="file" class="form-control" name="image">
          <p class="mt-2">ไฟล์ปัจจุบัน: <%= menu.image %></p>
          <img src="./images/menus/<%= menu.image %>" alt="<%= menu.image %>" class="img-thumbnail img-preview">
        </div>

        <div class="mb-3">
          <label>รายละเอียด</label>
          <textarea class="form-control" name="description" rows="4"><%= menu.description %></textarea>
        </div>

        <div class="mt-4 d-flex justify-content-between">
          <a href="/manage" class="btn btn-success">ย้อนกลับ</a>
          <button type="submit" class="btn btn-primary">บันทึก</button>
        </div>
      </form>
    </div>
  </div>
</body>

</html>
