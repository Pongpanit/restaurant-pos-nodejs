<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= title %>
    </title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        body {
            background-color: #f8f9fa;
            color: #212529;
        }

        .search-form {
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }

        .card:hover {
            transform: translateY(-5px);
            transition: 0.3s ease;
        }

        .card-img-top {
            height: 180px;
            object-fit: cover;
        }

        .category-header {
            font-size: 1.5rem;
            font-weight: bold;
            color: #343a40;
            border-bottom: 2px solid #0d6efd;
            padding-bottom: 10px;
            margin-top: 40px;
            margin-bottom: 20px;
        }

        /* แก้ไขตรงนี้เพื่อให้หมวดหมู่เลื่อนได้แนวนอน */
        .category-items {
            display: flex !important;
            flex-wrap: nowrap;
            overflow-x: auto;
            gap: 1rem;
            padding-bottom: 1rem;
            scroll-behavior: smooth;
        }

        .category-items::-webkit-scrollbar {
            height: 8px;
        }

        .category-items::-webkit-scrollbar-thumb {
            background-color: #0d6efd;
            border-radius: 4px;
        }

        .category-items::-webkit-scrollbar-track {
            background: transparent;
        }

        .category-items .menu-item {
            flex: 0 0 auto;
            width: 300px;
        }

        .dark-mode {
            background-color: #121212 !important;
            color: #f1f1f1 !important;
        }

        .dark-mode .navbar {
            background-color: #1f1f1f !important;
        }

        .dark-mode .navbar .nav-link {
            color: #ffffff !important;
        }

        .dark-mode .search-form {
            background-color: #1e1e1e;
            color: white;
            border: 1px solid #333;
        }

        .dark-mode .card {
            background-color: #1e1e1e;
            color: #ffffff;
            border: 1px solid #2a2a2a;
        }

        .menu-title {
            font-weight: 600;
            font-size: 1.1rem;
        }

        .text-price {
            color: #0d6efd;
        }

        .menu-description {
            font-size: 0.95rem;
        }

        .dark-mode .menu-title,
        .dark-mode .menu-description {
            color: #ffffff;
        }

        .dark-mode .modal-content {
            background-color: #2a2a2a;
            color: white;
        }

        .dark-mode .btn-close {
            filter: invert(1);
        }
    </style>
</head>

<body>
    <%- include('navbar.ejs') %>

        <div class="container py-4">
            <div class="mb-4 row align-items-center">
                <div class="mb-2 col-md-8">
                    <div class="search-form">
                        <input type="text" id="searchInput" class="form-control" placeholder="🔍 ค้นหาเมนู...">
                    </div>
                </div>
                <div class="col-md-4">
                    <select id="categorySelect" class="form-select">
                        <option value="All">ทุกหมวดหมู่</option>
                        <option value="อาหารเรียกน้ำย่อย">อาหารเรียกน้ำย่อย</option>
                        <option value="อาหารจานหลัก">อาหารจานหลัก</option>
                        <option value="เครื่องเคียง">เครื่องเคียง</option>
                        <option value="อาหารหวาน">อาหารหวาน</option>
                        <option value="เครื่องดื่ม">เครื่องดื่ม</option>
                    </select>
                </div>
            </div>

            <% let categories=["อาหารเรียกน้ำย่อย", "อาหารจานหลัก" , "เครื่องเคียง" , "อาหารหวาน" , "เครื่องดื่ม" ]; %>

                <div id="menuContainer">
                    <% categories.forEach(function (category) { %>
                        <div class="category-wrapper" data-category="<%= category %>">
                            <div class="category-header">
                                <%= category %>
                            </div>
                            <div class="category-items">
                                <% Object.values(menus).forEach(function (item) { %>
                                    <% if (item.status==='Active' && item.category===category) { %>
                                        <div class="menu-item" data-name="<%= item.name %>"
                                            data-description="<%= item.description %>"
                                            data-category="<%= item.category %>"
                                            data-image="./images/menus/<%= item.image %>"
                                            data-price="<%= item.price %>">
                                            <div class="shadow-sm card h-100">
                                                <img class="card-img-top menu-clickable"
                                                    src="./images/menus/<%= item.image %>" alt="<%= item.name %>"
                                                    data-name="<%= item.name %>"
                                                    data-image="./images/menus/<%= item.image %>"
                                                    data-price="<%= item.price %>"
                                                    data-description="<%= item.description %>">
                                                <div class="text-center card-body">
                                                    <h5 class="mb-2 card-title">
                                                        <a href="#"
                                                            class="menu-title text-decoration-none menu-clickable"
                                                            data-name="<%= item.name %>"
                                                            data-image="./images/menus/<%= item.image %>"
                                                            data-price="<%= item.price %>"
                                                            data-description="<%= item.description %>">
                                                            <%= item.name %>
                                                        </a>
                                                    </h5>
                                                    <p class="card-text text-price fw-bold">ราคา: <%= item.price %> บาท
                                                    </p>
                                                    <p class="card-text menu-description">
                                                        <%= item.description %>
                                                    </p>
                                                </div>
                                            </div>
                                        </div>
                                        <% } %>
                                            <% }) %>
                            </div>
                        </div>
                        <% }) %>
                </div>
        </div>

        <div class="modal fade" id="menuModal" tabindex="-1" aria-labelledby="menuModalLabel" aria-hidden="true">
            <div class="modal-dialog modal-dialog-centered">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="menuModalLabel">ชื่อเมนู</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="ปิด"></button>
                    </div>
                    <div class="modal-body text-center">
                        <img id="modalImage" src="" alt="" class="img-fluid mb-3"
                            style="max-height: 200px; object-fit: cover;">
                        <p id="modalPrice" class="fw-bold text-primary"></p>
                        <p id="modalDescription"></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <script>
            // เลือกหมวดหมู่
            document.getElementById('categorySelect').addEventListener('change', function () {
                filterMenus();
            });
        
            // ค้นหาเมนู
            document.getElementById('searchInput').addEventListener('keyup', function () {
                filterMenus();
            });
        
            function filterMenus() {
                const filter = document.getElementById('searchInput').value.toLowerCase();
                const selectedCategory = document.getElementById('categorySelect').value;
                const menuItems = document.querySelectorAll('.menu-item');
                const categoryWrappers = document.querySelectorAll('.category-wrapper');
        
                // แสดงหรือซ่อนเมนูตามคำค้นและหมวดหมู่
                menuItems.forEach(function (item) {
                    const name = item.dataset.name.toLowerCase();
                    const description = item.dataset.description.toLowerCase();
                    const category = item.dataset.category;
        
                    const matchText = name.includes(filter) || description.includes(filter);
                    const matchCategory = selectedCategory === 'All' || selectedCategory === category;
        
                    item.style.display = matchText && matchCategory ? 'block' : 'none';
                });
        
                // แสดงหรือซ่อนหมวดหมู่ที่ไม่มีเมนูแสดงผล
                categoryWrappers.forEach(function (wrapper) {
                    const items = wrapper.querySelectorAll('.menu-item');
                    let hasVisible = false;
                    items.forEach(function (item) {
                        if (item.style.display !== 'none') {
                            hasVisible = true;
                        }
                    });
                    wrapper.style.display = hasVisible ? 'block' : 'none';
                });
            }
        
            // Modal แสดงรายละเอียดเมนู
            document.querySelectorAll('.menu-clickable').forEach(function (element) {
                element.addEventListener('click', function (e) {
                    e.preventDefault();
                    const name = this.dataset.name;
                    const image = this.dataset.image;
                    const price = this.dataset.price;
                    const description = this.dataset.description;
        
                    document.getElementById('menuModalLabel').innerText = name;
                    document.getElementById('modalImage').src = image;
                    document.getElementById('modalImage').alt = name;
                    document.getElementById('modalPrice').innerText = `ราคา: ${price} บาท`;
                    document.getElementById('modalDescription').innerText = description;
        
                    const modal = new bootstrap.Modal(document.getElementById('menuModal'));
                    modal.show();
                });
            });
        </script>        

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>