<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= title %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    .card {
      border-radius: 1rem;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.1);
    }

    .card-header {
      background-color: #0d6efd;
      color: white;
      font-weight: bold;
      font-size: 1rem;
      border-top-left-radius: 1rem;
      border-top-right-radius: 1rem;
    }

    .table th,
    .table td {
      vertical-align: middle;
    }

    .empty-state {
      padding: 50px 0;
      text-align: center;
      font-size: 1.25rem;
      color: #888;
    }

    .empty-state i {
      font-size: 3rem;
      color: #ccc;
      margin-bottom: 15px;
    }

    .total-price {
      font-weight: bold;
      font-size: 1.25rem;
      color: #212529;
    }

    body.dark-mode .total-price {
      color: #f1f1f1;
    }
  </style>
</head>

<body>
  <%- include('navbar.ejs') %>

    <div class="container mt-5">
      <h1 class="text-center text-primary fw-bold mb-4">
        <%= title %>
      </h1>

      <!-- ถ้ามีออเดอร์ -->
      <% if (orders.length> 0) { %>
        <% orders.forEach((order, index)=> { %>
          <div class="card mb-4">
            <div class="card-header">
              ออเดอร์ที่ <%= index + 1 %> - วันที่: <%= order.createdAt.toLocaleString() %>
            </div>

            <div class="card-body">
              <% let totalPrice=0; %>
              <!-- ตารางแสดงรายการอาหารในออเดอร์ -->
                <table class="table table-striped table-hover text-center align-middle">
                  <thead class="table-light">
                    <tr>
                      <th>ชื่อเมนู</th>
                      <th>จำนวน</th>
                      <th>ราคาต่อชิ้น</th>
                      <th>ราคารวม</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% order.items.forEach(item=> { %>
                      <% if (item.menu) { const subtotal=item.menu.price * item.quantity; totalPrice +=subtotal; %>
                        <tr>
                          <td>
                            <%= item.menu.name %>
                          </td>
                          <td>
                            <%= item.quantity %>
                          </td>
                          <td>
                            <%= item.menu.price %> บาท
                          </td>
                          <td>
                            <%= item.menu.price * item.quantity %> บาท
                          </td>
                        </tr>
                        <% } %>
                          <% }) %>
                  </tbody>
                </table>

                <div class="text-end total-price mt-3">
                  ราคารวมทั้งหมด: <%= totalPrice %> บาท
                </div>                

                <!-- ปุ่มดูใบเสร็จ -->
                <div class="text-end">
                  <a href="/receipt/<%= order._id %>" class="btn btn-outline-primary btn-sm mt-2">
                    <i class="fas fa-receipt"></i> ดูใบเสร็จ
                  </a>
                </div>
            </div>
          </div>
          <% }) %>
            <% } else { %>
              <!-- ถ้าไม่มีออเดอร์ -->
              <div class="empty-state">
                <i class="fas fa-clipboard-list"></i>
                <p>ยังไม่มีออเดอร์ในระบบ</p>
              </div>
              <% } %>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/js/all.min.js"></script>
</body>

</html>