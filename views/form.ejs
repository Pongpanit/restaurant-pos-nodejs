<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    body {
      background-color: #f8f9fa;
      color: #212529;
      transition: background-color 0.3s, color 0.3s;
    }

    .form-container {
      background-color: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    }

    .form-group label {
      font-weight: 500;
      margin-bottom: 5px;
    }

    .btn-primary {
      padding: 10px 25px;
    }

    /* 🌙 Dark Mode */
    .dark-mode {
      background-color: #121212 !important;
      color: #f1f1f1 !important;
    }

    .dark-mode .form-container {
      background-color: #1e1e1e;
      color: white;
      border: 1px solid #333;
    }

    .dark-mode .form-control {
      background-color: #2a2a2a;
      color: white;
      border: 1px solid #444;
    }

    .dark-mode .form-control::placeholder {
      color: #bbb;
    }

    .dark-mode textarea.form-control {
      background-color: #000000;
      color: white;
    }

    .dark-mode .btn-primary {
      background-color: #0d6efd;
      border: none;
    }

    .dark-mode .btn-primary:hover {
      background-color: #0b5ed7;
    }
  </style>
</head>

<body>
  <%- include('navbar.ejs') %>

  <div class="container my-5">
    <div class="mx-auto form-container col-md-8">
      <h4 class="mb-4 fw-bold">เพิ่มเมนูอาหารใหม่</h4>

      <form method="POST" action="/insert" enctype="multipart/form-data">
        <div class="mb-3">
          <label>ชื่อเมนูอาหาร</label>
          <input type="text" class="form-control" name="name" placeholder="ใส่ชื่อเมนู">
        </div>

        <div class="mb-3">
          <label>ราคา</label>
          <input type="text" class="form-control" name="price" placeholder="ใส่ราคาเป็นตัวเลข">
        </div>

        <div class="mb-3">
          <label>หมวดหมู่อาหาร</label>
          <select class="form-select" name="category">
            <option value="อาหารเรียกน้ำย่อย">อาหารเรียกน้ำย่อย</option>
            <option value="อาหารจานหลัก">อาหารจานหลัก</option>
            <option value="เครื่องเคียง">เครื่องเคียง</option>
            <option value="อาหารหวาน">อาหารหวาน</option>
            <option value="เครื่องดื่ม">เครื่องดื่ม</option>
          </select>
        </div>

        <div class="mb-3">
          <label>ภาพเมนู</label>
          <input type="file" class="form-control" name="image">
        </div>

        <div class="mb-3">
          <label>รายละเอียด</label>
          <textarea class="form-control" name="description" rows="4" placeholder="ใส่รายละเอียดเกี่ยวกับเมนู"></textarea>
        </div>

        <div class="text-end">
          <button type="submit" class="btn btn-primary">บันทึก</button>
        </div>
      </form>
    </div>
  </div>
</body>

</html>
